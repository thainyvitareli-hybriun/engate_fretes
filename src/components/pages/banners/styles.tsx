import {StyleSheet} from 'pr-unit';

export default StyleSheet.create({
  container: {
    height: 400,
    alignItems: 'center',
    justifyContent: 'center',
    marginTop: -120,
  },
  image: {
    width: '100%',
    height: 300,
    resizeMode: 'contain',
    borderRadius: 10,
  },
});
